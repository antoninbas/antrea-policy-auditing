apiVersion: v1
kind: PersistentVolume
metadata:
  name: audit-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/log/audit"
